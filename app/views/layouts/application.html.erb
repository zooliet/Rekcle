<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>렉클</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' unless @app %>
    <%= stylesheet_pack_tag @app if @app %>
  </head>

  <body>
    <%= render 'header' %>
  	<main>
  		<%= yield %>
  	</main>
  	<%= render 'footer' %>
    <%= javascript_pack_tag @app if @app %>
  </body>
</html>
